<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:local="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    android:paddingRight="10dp"
    local:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
    local:popupTheme="@style/ThemeOverlay.AppCompat.Light">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <ImageView
        android:layout_width="90dp"
        android:layout_height="40dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:id="@+id/icon"
        android:src="@drawable/logo_gograbit"/>


    <ImageView
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:paddingLeft="7dp"
        android:paddingRight="7dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:id="@+id/toggleMap"
        android:src="@drawable/icon_toggle_map_view"/>
        <ImageView
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:paddingLeft="7dp"
            android:paddingRight="7dp"
            android:layout_centerVertical="true"
            android:id="@+id/searchMap"
            android:layout_toLeftOf="@id/toggleMap"
            android:src="@drawable/icon_serach"/>
        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:paddingLeft="7dp"
            android:paddingRight="7dp"
            android:layout_centerVertical="true"
            android:layout_toLeftOf="@id/searchMap"
            android:id="@+id/stNav"
            android:src="@drawable/icon_navigation"/>
    </RelativeLayout>
</android.support.v7.widget.Toolbar>
